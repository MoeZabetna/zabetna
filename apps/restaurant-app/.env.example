# Copy to .env and fill in. Same two values as apps/user-app — all three
# clients share one Supabase project. Never put the service role key in an
# EXPO_PUBLIC_* variable: it bypasses RLS entirely.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
