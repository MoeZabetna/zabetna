import { ComingSoon } from "@/components/ComingSoon";

export default function AdminsPage() {
  return <ComingSoon title="Admins" note="Invite admins and assign roles — admin_roles/role_permissions already exist." />;
}
