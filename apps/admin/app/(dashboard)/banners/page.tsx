import { ComingSoon } from "@/components/ComingSoon";

export default function BannersPage() {
  return <ComingSoon title="Banners" note="In-app promotional banners — schema and RLS already exist (banners table)." />;
}
